export const REPORT_TYPES = Object.freeze({
  MACHINE_PERFORMANCE: 'machine-performance',
});

export const REPORT_RANGE_TYPES = Object.freeze({
  DAILY: 'daily',
  LAST_MONTH: 'last_month',
});

export const REPORT_FORMATS = Object.freeze({
  PDF: 'pdf',
  XLSX: 'xlsx',
});

export const REPORT_STATUSES = Object.freeze({
  PROCESSING: 'processing',
  READY: 'ready',
  FAILED: 'failed',
});

export const DEFAULT_REPORT_DIRECTORY = 'storage/reports';
export const REPORT_TYPES = Object.freeze({
  MACHINE_PERFORMANCE: 'machine-performance',
});

export const REPORT_RANGE_TYPES = Object.freeze({
  DAILY: 'daily',
  LAST_MONTH: 'last_month',
});

export const REPORT_FORMATS = Object.freeze({
  PDF: 'pdf',
  XLSX: 'xlsx',
});

export const REPORT_STATUSES = Object.freeze({
  PROCESSING: 'processing',
  READY: 'ready',
  FAILED: 'failed',
});

export const DEFAULT_REPORT_DIRECTORY = 'storage/reports';

